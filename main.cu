#include <kernels.cuh>
#include <utils.cuh>

int main () {
	// define the problem size M, N, K

	// allocate memory on host

	// initialize values for matrices

	// allocate memory on device

	// copy matrices from host to device

	// execute kernels

	// profile performance, on different problem sizes

	return 0;
}
